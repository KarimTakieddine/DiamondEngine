#pragma once

#include <vector>

/*
#include "Controller.h"

namespace diamond_engine
{
	class ControllerConfig
	{
	public:
		struct ButtonConfig
		{
			std::string name;
			Button button;
		};

		struct JoystickConfig
		{
			std::string name;
			Joystick joystick;
		};
		
		const std::vector<ButtonConfig>& getButtonConfigs() const;
		void addButtonConfig(const ButtonConfig& buttonConfig);

		const std::vector<JoystickConfig>& getJoystickConfigs() const;
		void addJoystickConfig(const JoystickConfig& joystickConfig);

		float getJoystickDeadzone() const;
		void setJoystickDeadzone(float joystickDeadzone);

	private:
		std::vector<ButtonConfig> m_buttonConfigs;
		std::vector<JoystickConfig> m_joystickConfigs;
		float m_joystickDeadzone{ 0.0f };
	};
}
*/